apiVersion: v1
kind: Service
metadata:
  name: push-notification
  namespace: i442651
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000  
    name: rest
    protocol: TCP
  selector:
    app: push-notification